# Generated data files: never worth a manual merge — the pipeline regenerates
# them every 15 minutes. The "keepours" driver auto-resolves conflicts by
# keeping the local version. Requires one-time setup per clone (sync.sh does it):
#   git config merge.keepours.driver true
data/*.json merge=keepours
data/archive/*.json merge=keepours
